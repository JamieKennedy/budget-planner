export enum FormState {
    Pending,
    Errored,
    Default,
}

export enum AuthState {
    Success,
    Failure,
    Pending,
}

export enum PageState {
    Loading,
    Loaded,
    AuthError,
}

export enum TrendDirection {
    Up,
    Down,
    Level,
}
